using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DestroyWool : MonoBehaviour
{
    private bool isToBeDestroyed = false;
    // Start is called before the first frame update
    void Start()
    {
       // Time.cu
       // Time.deltaTime;
    }

    // Update is called once per frame
    void Update()
    {
        if (isToBeDestroyed)
        {

        }
    }

    public void startDestroyTimer() //moze da se poboljsa tako sto se brise kad je van pogleda kamere
    {
        
    }
}
